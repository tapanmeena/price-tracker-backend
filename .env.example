# Example environment variables (DO NOT commit real secrets)
NODE_ENV=production
PORT=3333
# Replace with your DB connection string; do NOT use real credentials here
DATABASE_URL=postgresql://username:password@db:5432/pricetracker_db?schema=pricetracker
ALLOWED_ORIGINS=*
# Replace with strong random secrets in your local .env or CI settings
JWT_ACCESS_SECRET=replace_with_random_access_secret
JWT_REFRESH_SECRET=replace_with_random_refresh_secret
